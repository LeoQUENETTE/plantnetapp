<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.plantnetapp">

    <!-- Si tu utilises la caméra in-app -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Plantnetapp">  <!-- ton thème NoActionBar -->

        <!-- Launcher : LoginActivity -->
        <activity
            android:name=".front.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Autres activités -->
        <activity android:name=".front.RegisterActivity"    android:exported="false"/>
        <activity android:name=".front.MainActivity"        android:exported="false"/>
        <activity android:name=".front.PhotoActivity"       android:exported="false"/>
        <activity android:name=".front.HistoriqueActivity"  android:exported="false"/>
        <activity android:name=".front.PlantsListActivity"  android:exported="false"/>
        <activity android:name=".front.DetailActivity"      android:exported="false"/>

    </application>
</manifest>
